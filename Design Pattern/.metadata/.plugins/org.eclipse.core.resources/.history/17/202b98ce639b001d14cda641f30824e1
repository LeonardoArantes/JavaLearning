package br.com.alura.loja.discount;

import java.math.BigDecimal;

import br.com.alura.loja.budjet.Budget;

public abstract class Discount {
	
	protected Discount next;

	public Discount(Discount next) {
		super();
		this.next = next;
	}
	
	public BigDecimal calculating(Budget budget){
		if(isApplicable(budget)) {
			return calculate(budget);
		}
		
		return next.calculating(budget);
	}
	
	protected abstract BigDecimal calculate(Budget budget);//calcular	
	protected abstract boolean isApplicable(Budget budget);//deveaplicar?
}
